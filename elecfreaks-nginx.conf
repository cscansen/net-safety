server {
    listen 8080;
    server_name localhost;

    root /var/www/elecfreaks-kb;
    index index.html;

    # Redirect bare root to the mirrored section
    location = / {
        return 302 /en/microbit/;
    }

    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
